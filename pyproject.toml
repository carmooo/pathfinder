[tool.poetry]
name = "pathtime"
version = "0.1.0"
description = ""
authors = ["jvc19"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
black = "^23.3.0"
isort = "^5.12.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
